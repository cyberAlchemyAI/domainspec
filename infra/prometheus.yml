global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts/knowledge-graph-visualization.rules.yml

scrape_configs:
  - job_name: domainspec-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:3000
