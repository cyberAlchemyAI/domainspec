---
node_type: craft-view
title: CRAFT — Deterministic Test-Derivation Engine
status: active
updatedAt: 2026-06-15
---

# CRAFT — Deterministic Test-Derivation Engine

> Human view of [`.craft/ledger.yml`](.craft/ledger.yml) (the source of truth). Do not edit this file as authority — update the ledger and re-export.

**Context `CTX-TDE-ROOT` — stage: `implement` · gate: `flag`**

Build a fully deterministic, no-LLM engine that compiles a feature's canonical Markdown docs into byte-stable test obligations (`parse → G → δ → obligation_key → emit`), making DomainSpec claim **C2 (determinism)** true _by construction_ and replacing the LLM-backed `domainspec-generate-tests`.

## Quick links

- **Next move (P1):** `task-session` honesty foundation — add the negative-control gate (INV-3) + split WF/QT/MT bucketing to per-row identity (INV-1) so the round-trip is provably falsifiable BEFORE generalizing bridging (`BLK-TDE-GATE-HONESTY-001`). Then P2 general bridging + **new δ rules** for error/consumer-event obligations (`BLK-TDE-AUTH-CONVENTION-001`). Plan: [2026-06-21 refine RESULT](../../development/refinement-runs/2026-06-21-c2-engine-to-evidence/RESULT.md).
- **Active blockers:** `BLK-TDE-GATE-HONESTY-001` (honesty foundation, P1), `BLK-TDE-AUTH-CONVENTION-001` (bridging + new δ, P2)
- **Open decisions:** none (`DEC-TDE-SEMANTIC-RIGOR-001` closed → SPLIT)
- **Active gaps:** `GAP-TDE-EMITTESTS-BODIES-001`, `GAP-TDE-POSTCONDITION-IDS-001` (bumped low→medium)
- **Readiness gate:** `GATE-TDE-CORPUS-GENERALIZATION-001`
- **Resolved:** `BLK-TDE-COMMIT-DISCIPLINE-001` ✅ (committed `ff1eba0` + pushed; parent bumped `af15e4d`)

## Current state

L0 falsification gate **PASSES** for `financial-settlement`: round-trip reproduces 100% of committed obligations (missing=0) with 16 legitimate completeness extras, byte-identical across runs, `tsc` 0, 33/33 tests. L1 partial: `emit_tests` + linter done, 2nd feature parses (108 obligations, 0 violations) but its round-trip **FAILs** on oracle-convention drift. See [L0-ROUNDTRIP-REPORT](../../development/deterministic-test-derivation-engine/L0-ROUNDTRIP-REPORT.md).

## Decisions

| ID                           | Question                                 | Status | Selected                                                                            | Evidence                                                                                          |
| ---------------------------- | ---------------------------------------- | ------ | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `DEC-TDE-HOME-001`           | Where should the engine live?            | closed | `tools/test-derivation-engine/`                                                     | [ARCH](../../development/deterministic-test-derivation-engine/ARCHITECTURE.md)                    |
| `DEC-TDE-LANG-001`           | Implementation language?                 | closed | TypeScript                                                                          | [ARCH](../../development/deterministic-test-derivation-engine/ARCHITECTURE.md)                    |
| `DEC-TDE-INPUT-SCOPE-001`    | 4 docs or all 7?                         | closed | Extend to all 7                                                                     | [L0-REPORT](../../development/deterministic-test-derivation-engine/L0-ROUNDTRIP-REPORT.md)        |
| `DEC-TDE-SEMANTIC-RIGOR-001` | Per-row vs concept-bucket gate identity? | closed | **SPLIT** — accept op-bucket for postconditions; reject concept-bucket for WF/QT/MT | [2026-06-21 refine](../../development/refinement-runs/2026-06-21-c2-engine-to-evidence/RESULT.md) |

> **Refine 2026-06-21:** the passing oracle dialect (`rv-ct`) is the corpus _minority_ (2/7); the failing `column-typed` dialect is the _majority_ (5/7). The financial PASS is partly concept-presence-only (non-injective WF/QT/MT folds), and ~12+ auth misses are _genuine δ gaps_ (error/consumer-event obligations), not just convention drift — hence the P1 honesty-first ordering.

## Blockers · enablers · gate

- `ENA-TDE-L0-PASS-001` (enabler) — the passing financial-settlement L0 gate is the proof input for C2-by-construction.
- `BLK-TDE-AUTH-CONVENTION-001` — auth round-trip FAILs (30 missing / 25 extra) on dialect drift (op-qualified ids, `Session` vs `SessionLifecycle`, row-id-keyed transitions, Error-state category, consumer-bucketed events). δ sound; identity bridging not yet general. → `GATE-TDE-CORPUS-GENERALIZATION-001`.
- `BLK-TDE-COMMIT-DISCIPLINE-001` (requires human) — engine + docs uncommitted; earlier submodule commit unpushed and parent gitlink unbumped. Push is operator-gated.
- `GATE-TDE-CORPUS-GENERALIZATION-001` — engine must round-trip ≥N features (not just financial-settlement) before "deterministic derivation" generalizes to the corpus.

## Gaps

- `GAP-TDE-EMITTESTS-BODIES-001` (medium, defer) — emitted vitest cases are `expect.fail` stubs; real bodies needed before E3 mutation use.
- `GAP-TDE-POSTCONDITION-IDS-001` (low, accept) — PC/WF/QT/MT rows lack source ids → gate bridges at concept-bucket level; per-row rigor needs an upstream oracle authoring fix.

## Definitions (candidate)

`deterministic test-derivation engine` · `obligation_key` · `semantic round-trip identity` · `needs_formal` — see [`.craft/ledger.yml`](.craft/ledger.yml) `definitions`.

## Artifacts

[engine package](.) · [SPEC](../../development/deterministic-test-derivation-engine/SPEC.md) · [ARCHITECTURE](../../development/deterministic-test-derivation-engine/ARCHITECTURE.md) · [GLOSSARY](../../development/deterministic-test-derivation-engine/GLOSSARY.md) · [WORK-PACK](../../development/deterministic-test-derivation-engine/WORK-PACK.md) · [L0-ROUNDTRIP-REPORT](../../development/deterministic-test-derivation-engine/L0-ROUNDTRIP-REPORT.md) · [refine RESULT](../../development/refinement-runs/2026-06-12-test-derivation-c2-cluster/RESULT.md)
