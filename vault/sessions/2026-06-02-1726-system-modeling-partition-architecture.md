---
tags: [folder-structure, partition-architecture, ownership-axis, arcanum, ontology, architecture, vault]
node_type: discovery
is_session: true
layer: ontology, architecture
nature: explanatory, technical
status: active
created: 2026-06-02
timestamp: "2026-06-02T17:26:01-03:00"
expires: 2026-08-01
conversation_id: unknown
decisions_made: true
contradictions_found: false
specs_updated: []
promoted_candidates: []
expected_importance: 6
importance_rationale: "Crystallizes the top-level partition axis (ownership) and the arcanum-as-executable-projection distinction that the future external-app trajectory depends on, but conviction is low and the decision is tentative."
---

# System-Modeling Partition Architecture

## Summary

The session researched prior work across sibling repos on folder structure / structural self-similarity / fractality (via parallel explorers + two summarizers), then designed a partition architecture for modeling any system "our way". It decided — tentatively, low conviction — that the top-level primary directory axis is **ownership** (shared-framework vs per-project, with arcanum + system_design_knowledge on a trajectory to factor out into a future external app), with subject-partitions, sessions as a distinct modality, arcanum as the executable projection of system_design_knowledge, and a migration model separating vertical promotion from horizontal routing. The work was captured as a new `draft` discovery `system-modeling-partition-architecture` with bidirectional `derives-from`/`derives` edges to its three prior-art discoveries; all four nodes pass schema validation.

## Contradictions

- Revisits `vault/ontology-conventions.md` §8: `edges-check` recognizes only forward edge names, so the canonical inverse `derives` is flagged "unknown" across the whole vault — a tooling-vs-catalog drift (the documented 0/10-enforced reality), not a content contradiction.

## Files touched

- vault/discovery/system-modeling-partition-architecture/discovery.md
- vault/discovery/folder-structure-fractal/discovery.md
- vault/discovery/cross-tree-mirroring-for-llm-coercion/discovery.md
- vault/discovery/schema-of-schemas/discovery.md

## Connections

| Document | Type | Description |
|----------|------|-------------|
| `vault/discovery/system-modeling-partition-architecture/discovery.md` | `creates` | This session produced the partition-architecture discovery as a new file. |
| `vault/discovery/folder-structure-fractal/discovery.md` | `modifies` | This session edited the discovery, adding an inverse `derives` edge row. |
| `vault/discovery/cross-tree-mirroring-for-llm-coercion/discovery.md` | `modifies` | This session edited the discovery, adding an inverse `derives` edge row. |
| `vault/discovery/schema-of-schemas/discovery.md` | `modifies` | This session edited the discovery, adding an inverse `derives` edge row. |
