# Session — Agent-Context Boundary Rule (best CLAUDE.md)

Goal: design a portable cross-repo RULE for authoring the best possible CLAUDE.md + MEMORY.md, as a discovery (route 3). Derived from existing material, not new doctrine.

- [DECISION] Form = discovery first; scope = portable cross-repo (user-confirmed via AskUserQuestion).
- [INSIGHT] "Best CLAUDE.md" splits by enforceability: I7 bridges give the *qualities* (mostly semantic/judgment); harness-as-enforcement-layer proves CLAUDE.md prose is best-effort, only hooks enforce. A single prose rule rots — it IS the "corrupted boundary" anti-pattern. So the rule must be a PAIR: Layer A (enforced floor / validator) + Layer B (judgment ceiling / skill).
- [INSIGHT] The rule is not new doctrine — it operationalizes AX-ONT-1/3/4/6/7 applied to the boundary object (CLAUDE.md + MEMORY.md = ∂B). I7 already maps each property to an axiom.
- [INSIGHT] I7's gift: the framework's real MEMORY.md failures "take the form of broken dependency edges, not missing content" → that IS mechanically checkable. Broken-edge + budget + pointer-shape + no-transient + route-resolves = the enforceable floor.
- [INSIGHT] Honest ceiling: CLAUDE.md has no horizon; compression is engineered, not forced by geometry; addressability ≠ equivalence. Rule cannot guarantee "best", only (a) enforce faithfulness floor, (b) make judgment explicit/reviewable.
- [DECISION] Source material lives in repo domainspec-lean-formalization (I7 bridges) — cross-repo citation; no vault inverse edge (external repo, per curator-pipeline OQ-C).
- [QUESTION] Cross-repo distribution mechanism (copy / submodule / Arcanum-style generated installer)? Defer to cross-repo-rollout canonicalization OQ; pilot in domainspec.
- [QUESTION] Does CLAUDE.md get a machine-readable route manifest block for the validator to parse? Lean toward fenced block, not full frontmatter.
- [QUESTION] Run I7's falsifiable prediction (paired-conversation divergence ∝ D_KL) as an experiment to validate "density matters" before over-investing? Register as experiment, don't gate the rule on it.
- Output: vault/discovery/agent-context-boundary-rule/discovery.md
