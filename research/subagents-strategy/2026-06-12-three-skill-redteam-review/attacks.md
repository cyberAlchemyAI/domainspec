# Attacks

Three red-team attacker returns, verbatim, under per-attacker headers. Synthesized in `findings.md`.

## Gödel, Kurt — governance fidelity (does each derived doc faithfully restate the constitution it points to?)

Findings: 1) MAJOR research/SKILL.md L35 "auditor runs last" is stale sequential-order phrasing vs amended P4 dependency scheduling. 2) MAJOR research L68 "after the reviewers" same defect. 3) MAJOR research L70-72 "Early stop ... ends the dispatch early" silently introduces a lifecycle exit not in the closed exit_reason vocab / doesn't name resolved or invoke final_approver. 4) MAJOR research L125-126 "resolved = final_approver accepted; for research acceptance includes the P9 citation check" tightens §5's bare 'resolved' definition (CONTESTED — Gödel's own dissent: P9 L133 ties citation check to approver acceptance, may be faithful composition; downgrade candidate). 6) MAJOR router L28 P7 restatement omits the n=1 'returns its single output' carve-out (§5 P7 L131). 7) MINOR router L18 drops "and registration" from declared-order tiebreak (§3 L73). 8) MINOR research L83 same "and registration" omission. 9) MAJOR register-dispatch L50 'records anyway' permits a recorded reserved-type dispatch the constitution forbids (§5 L165 must-not-be-dispatched). 10) MINOR register-dispatch L211 loops_used hardened to required-with-type beyond §5's example-only. 11) MINOR register-dispatch L209 orphan-close 'still appends' is fail-soft beyond P3, unlabeled. 12) MAJOR research L99-100 green-light pairwise-evidence narrowed to 'explorer pair' but P5 test4 L128 applies to every pair in any n≥2 group (drops it for skeptic groups). 13) MINOR research L102-104 Dissent-line/zero-dissent flag is type-specific, fine if labeled. Did NOT find any surviving "groups run sequentially in declared order" — all lifecycle/run sections carry amended dependency text. Dissent: least sure on #4.

## Quine, Willard Van Orman — ownership & reference integrity (single-owner discipline; do pointers resolve; is anything re-narrated that should be pointed?)

Findings: 1) non-finding — feedback-edge endpoint clause survives intact in all docs that restate it. 2) MAJOR — dependency-scheduling readiness rule re-narrated NORMATIVELY (not pointed) in 4 docs: constitution §3/P4, router L18, research L78-83, review L87-91; D1 amendment had to be hand-propagated to all four; drift risk. Fix: type skills replace readiness paragraph with a one-line pointer to router/constitution P4. 3) MINOR — research L86-104 re-narrates P5 four-test rule beyond a pointer (but cites source, so labeled). 4) MINOR — router L54 points to vault/discovery/anti-bias-vector-composition/ WITHOUT the 'pending realignment / speaks removed schema' caveat the constitution carries; reader lands on stale schema unwarned. 5) MINOR — 'reviewers' (research) vs 'verifiers' (review) label split for the same evaluate slot; mapping is stated so traceable. 6) non-finding — full dangling-pointer sweep: ALL paths/sections resolve on disk (review SKILL, research SKILL, router, agent-pool.yaml, subagents-dispatch.yaml, robot-talks-constitution, live v0.3.0 constitution, premises v0.4.0, validator-check.md + siblings, register-dispatch + append-dispatch.cjs, assessment findings). 7) sibling-repo pointer correctly self-disclosed unverifiable. 8) VERIFIED TRUE — 245 names + ordered role_fit. 9) MINOR — register-dispatch L31-34 cites "constitution §7's removed table" as authority for keys (topic_slug, corpus, session) NOT in that table; separate §7-removed from pre-v0.5.2-ledger-only. 10) MINOR — invoked_by correctly single-owned by register-dispatch with constitution §9 tracking the pending amendment; transient gap. 11) MINOR — 'synthesizer alone in its group' (research L34, review L29) stated as inherited law but §5 nowhere forbids n≥2 on a synthesize group. Dissent: my MAJOR (Finding 2) is contestable — if operability outranks DRY, the deliberate identical 4-way restatement drops to MINOR; single-owner only forbids CONFLICTING re-narration, which I did not find.

## Abramsky, Samson — mechanics & operability (does the chain actually run; what does the appender accept that it should reject?)

Findings: 1) CRITICAL — register-dispatch SKILL L110-114 run command uses "$CLAUDE_PROJECT_DIR/.register-dispatch.tmp.json" but CLAUDE_PROJECT_DIR is unset in the live session → expands to /.register-dispatch.tmp.json → appender exit 2 'cannot read/parse record'; step 2 writes to <repo-root>/ but step 3 reads from $CLAUDE_PROJECT_DIR/; appender's internal root fallback does NOT rescue the temp-file arg. CONTESTED by own dissent: environment-specific (a harness that exports the var works, as the test battery does). Fix: pass temp file by the same path step 2 wrote (relative from repo root). 2) MAJOR — dependency scheduling enforced nowhere; PROBE: a sheet whose only edge into a group is `feedback` registers exit 0 and would launch that group empty (feedback never counts as dependency). 3) MAJOR — append-dispatch.cjs:240-243 agents_spawned.tree accepts arbitrary keys + negative/garbage values; total never reconciled vs tree (PROBE: tree:{bananas:99,nonsense:-5} exit 0; total:9999 exit 0). 4) MAJOR — feedback_prompts verbatim-record contract (P3) doc-only; close row validated in isolation, can omit feedback_prompts even when a feedback edge existed (exit 0). 5) MAJOR — router L39 'refuse and tell the user' for RESERVED type vs appender:361-363 'recording anyway' exit 0 (PROBE plan) vs register-dispatch L50 codifying record-anyway — three-way contradiction on the LIVE gate. 6) MAJOR — layers>1 on zig-zag/feedback endpoint forbidden §5 L266-271 but appender accepts (PROBE: rev layers:3 on zig-zag exit 0); mechanically checkable, appender has the inputs. 7) MINOR — no loop_cap vs max_loops coherence (PROBE max_loops:1 loop_cap:5 exit 0). 8) MINOR — register-dispatch L101-102 'hook blocks Bash even read-only' asserted not demonstrated; cite the hook matcher. 9) MINOR — P5 four-test anti-bias gate-checked by hand vs stale validator-check.md, zero executable backstop (appender only checks presence of anti_bias/angle). 10) MINOR — final_approver self-approval (P12) unenforced; PROBE final_approver = sole explorer's name exit 0; appender could structurally reject approver==any agent_name. Dissent: CRITICAL #1 contingent on CLAUDE_PROJECT_DIR unset; downgrade to MAJOR if harness exports it.
