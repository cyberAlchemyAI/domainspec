"""convergence_runner — replicable multi-agent dispatch with structured logging.

MVP: dispatch-only half. Pins prompt/corpus/agent-spec; captures raw output
and tool-call traces. Boundary classifier and metrics deferred per
`two-layer-platform-architecture` / lens 02 risk register #2 (wait until the
form-invariance metric is settled on paper first).
"""

__version__ = "0.1.0"
