"""graph_retrieval — two-layer retrieval prototype.

Per-intent compose-functions over a typed-edge subgraph. Body-similarity
+ graph-traversal + type/stage/verification filters per intent.

MVP: scaffolding + compose-function skeleton. Kuzu graph backend deferred
until the design has at least one corpus to test against.
"""

__version__ = "0.1.0"
