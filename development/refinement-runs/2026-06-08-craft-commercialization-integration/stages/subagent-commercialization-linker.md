# Stage Receipt — Commercialization Linker (Design / link to plan)

- Role: Commercialization Linker · agentId ad41b074e48c04e07 · verdict: **pass**

## Free/paid placement: tiered, right-way-round open-core

- **FREE (wedge):** the ledger schema, `.craft/ledger.yml` + Markdown view, local interface methods, base type/lane system, blocker-refinement discipline. A YAML ledger is _more_ clonable than the validators — paywalling it throws away the wedge. Free Craft makes the existing free wedge (sigils + Necronomicon + spec→test) _stickier_ via a governance trail.
- **PAID:** the **deferred** role-delegation automation (`condition type + lane → role → capability/human`), priority scoring, generated index, hosted reflection; plus the encoded judgment (pre-populated attested ledger templates for a regulated vertical).
- This is open-core **the right way around** (free format, paid governed loop) — directly answers the prior run's "open-core inversion" warning.

## R2 verdict: **PARTIAL** (the lever and surface, not yet the metric)

- FOR: Craft is literally a self-serve governance surface (run `start_project`, ledger accrues typed blockers/decisions/recompositions with no consultant) → escapes the consulting trap. Manufactures usage metrics that scale with value (governed contexts, refined blockers, recompositions) — the "governed runs" shape, not seats. Produces an auditable artifact = trust collateral.
- AGAINST: Craft is a **ledger, not a meter** — scoring/index/role-delegation are _deferred_; it can record governance but not yet _price_ it. Still prompt-driven, not enforced — an agent can ignore the ledger (R1 leaks into R2). Free self-served ledger can cannibalize the paid line unless paid = delegation + attestation + hosted reflection.
- **Net:** Craft converts R2 from "open question" to "build the deferred automation as the paid meter." It makes R2 solvable self-serve; it does not _close_ it.

## Promotion-DAG relationship: **COMPOSE** (different altitudes)

- CyberAlchemy DAG governs **knowledge claims** (raw→candidate→…→axiom, two-axis confidence). Craft governs **work contexts** (idea→define→…→reflect, recursive). Not competing (different nouns), not redundant (Craft lacks the DAG's confidence machinery; the DAG lacks recursion/recomposition). Clean seam: a Craft `decision`/`add_definition` should **emit a PromotionRecord draft** → DAG adjudicates → promoted policies feed back as Craft gates. Two ledgers, one governed lifecycle.

## R1 relevance: hardens, does not fix

Craft makes every spec→test run leave an immutable, typed, auditable governance trail = the "re-anchor on the proprietary governance-data asset" escape R1 offered. But Craft is itself a method/prompt discipline, not a compiler — it does **not** make the engine deterministic. Lean remains the only engine-determinism lever; Craft (process record) + Lean (proof) are complementary.

## Integration recommendation

1. Free Craft ledger into Arcanum now (companion to free wedge; dogfood on spec→test → generates both Craft promotion evidence and the R1 governance-data asset).
2. Build the **deferred automation as the paid tier**; bill on governed-context/refined-blocker/recomposition throughput.
3. Compose with the promotion DAG at the decision boundary (emit PromotionRecord drafts; sell the adjudicated half).
4. Bind Golden Quill to a pre-populated, attested Craft template (proprietary SKU; home for Lean attestation).
